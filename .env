FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=8080

# HTTPS Configuration
# Set to 'true' to enable HTTPS, 'false' to use HTTP
USE_HTTPS=false

# Path to SSL certificates (relative paths are resolved from the app directory)
# SSL_CERT_PATH=certificates/cert.pem
# SSL_KEY_PATH=certificates/key.pem

# Uncomment and set the above variables to enable HTTPS when running with 'flask run'
# Don't forget to create the certificates directory and generate certificates:
# mkdir -p certificates
# openssl req -x509 -newkey rsa:4096 -nodes -out certificates/cert.pem -keyout certificates/key.pem -days 365